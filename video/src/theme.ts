// Палитра ролика.
//
// Тёмная, а не светлая, как в админке: в ленте ролик чаще смотрят с телефона
// и в помещении, где светлый прямоугольник бьёт по глазам. Акценты те же,
// что у самого Hark, чтобы снимки экрана в кадре не выглядели чужими.
export const C = {
  bg: '#0b0d0f',
  bgSoft: '#14171a',
  line: 'rgba(255,255,255,0.10)',
  lineStrong: 'rgba(255,255,255,0.18)',
  t1: 'rgba(255,255,255,0.95)',
  t2: 'rgba(255,255,255,0.66)',
  t3: 'rgba(255,255,255,0.40)',
  accent: '#34d399',
  accentSoft: 'rgba(52,211,153,0.14)',
  // Тёплый — цвет рассуждения: того, за что платят и чего не видят.
  warm: '#fbbf24',
  warmSoft: 'rgba(251,191,36,0.13)',
};

export const F = {
  sans: '"Segoe UI", -apple-system, BlinkMacSystemFont, Roboto, sans-serif',
  mono: '"JetBrains Mono", Consolas, "SF Mono", monospace',
};

// Длительности сцен в кадрах при 30 кадрах в секунду.
//
// Ролик смотрят беззвучно: и Телеграм, и Reddit проигрывают его без звука,
// пока не ткнёшь. Значит, весь смысл несёт текст, а текст нужно успеть
// прочитать. Отсюда сцены по восемь-девять секунд, а не по три.
export const D = {
  hook: 150,
  receipt: 270,
  model: 240,
  data: 240,
  widget: 240,
  outro: 210,
};

export const totalFrames = D.hook + D.receipt + D.model + D.data + D.widget + D.outro;

// Пружина без перелёта: подпрыгивающий текст читать труднее.
export const EASE = [0.32, 0.72, 0, 1] as const;
